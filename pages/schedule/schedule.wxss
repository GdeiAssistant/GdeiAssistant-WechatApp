@import "../../common/lib/weui.wxss";

page, .page, .page__bd {
  height: 100%;
  background-color: var(--color-bg-primary);
}

.page__bd {
  padding-bottom: 0;
}

.weui-navbar {
  position: static;
  background-color: var(--color-surface);
}

.weui-tab__panel {
  padding-top: 0;
}

.weui-media-box__title {
  color: var(--color-link);
  font-size: 26rpx;
}

.weui-media-box {
  padding: 20rpx;
  margin-top: 20rpx;
  position: relative;
  background-color: var(--color-surface);
}

.weui-media-box__info {
  color: var(--color-text-primary);
  line-height: 1.6;
}

.weui-media-box__bold {
  font-size: 30rpx;
  font-weight: bolder;
}

text + .weui-media-box__bold {
  display: block;
  padding-top: 40rpx;
}

.weui-media-box__bold + text {
  float: right;
}

.weui-media-box:before {
  content: none;
}

.button-sp-area {
  padding: 20rpx 35rpx;
}

button[type="primary"][plain] {
  color: var(--color-surface);
  border: 1px solid #96deda;
  background-image: linear-gradient(-45deg, #96deda 0%, #8ec5fc 100%);
}

.button-hover[type="primary"][plain] {
  color: var(--color-surface);
  border-color: #96deda;
  opacity: 0.6;
}

button[type="default"][plain] {
  color: var(--color-surface);
  border: 1px solid #fbc2eb;
  background-image: linear-gradient(-45deg, #fbc2eb 0%, #a6c1ee 100%);
}

.button-hover[type="default"][plain] {
  color: var(--color-surface);
  border-color: #fbc2eb;
  opacity: 0.6;
}